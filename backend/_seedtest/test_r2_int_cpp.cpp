#include <bits/stdc++.h>
using namespace std;

int main() {
    string line;
    getline(cin, line);
    stringstream ss(line);
    vector<int> arr;
    int x;
    while (ss >> x) arr.push_back(x);

    int target;
    cin >> target;

    // TODO: implement your solution using arr and target
    cout << 0 << endl;
    return 0;
}
