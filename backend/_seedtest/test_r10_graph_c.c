#include <stdio.h>

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    int u[10000], v[10000];
    for (int i = 0; i < m; i++) scanf("%d %d", &u[i], &v[i]);

    // TODO: implement your solution using n, m, and edges u[i]-v[i]
    printf("false\n");
    return 0;
}
