#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    cin.ignore();
    vector<string> words(n);
    for (int i = 0; i < n; i++) getline(cin, words[i]);

    // TODO: implement your solution using words
    cout << 0 << endl;
    return 0;
}
