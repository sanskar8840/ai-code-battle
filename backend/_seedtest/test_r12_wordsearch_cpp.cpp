#include <bits/stdc++.h>
using namespace std;

int main() {
    int r, c;
    cin >> r >> c;
    vector<string> grid(r);
    for (int i = 0; i < r; i++) cin >> grid[i];
    string word;
    cin >> word;

    // TODO: implement your solution using grid and word
    cout << "false" << endl;
    return 0;
}
