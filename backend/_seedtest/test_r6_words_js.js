const fs = require("fs");

function solve() {
    const lines = fs.readFileSync(0, "utf-8").split("\n");
    const a = lines[0] || "";
    const b = lines[1] || "";

    // TODO: implement your solution using a and b
    console.log(0);
}

solve();
